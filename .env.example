# Build-time config, read only by src/main/infra/config.ts (docs/spec.md §8.3).
# Copy to .env.local (git-ignored) and fill in with the Desktop OAuth client
# from your own Google Cloud project. Never commit real values.
MAIN_VITE_GOOGLE_CLIENT_ID=
MAIN_VITE_GOOGLE_CLIENT_SECRET=
